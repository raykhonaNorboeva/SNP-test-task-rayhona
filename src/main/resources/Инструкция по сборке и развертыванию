Для установки Docker и Docker Compose, если они еще не установлены,
рекомендуется следовать инструкциям на официальном сайте Docker.
Чтобы собрать и запустить проект, необходимо перейти в корневую директорию проекта,
где находятся файлы Dockerfile и docker-compose.yml. Затем, чтобы собрать и запустить проект,
следует выполнить команду:
docker-compose up --build
После выполнения этой команды Docker Compose начнет сборку образа для бота
и поднимет контейнеры для бота и базы данных.